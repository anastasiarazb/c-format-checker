void main(void)
{
    if(i < n)
  diff = diff*x+a;  // Вложенное выражение левее объемлющего
    if (1 ? 1 : 0)
        if1  // Имя метки и двоеточие - одно утверждение
          :  // перенос
        printf("if 1 printf1\n");  // printf(...) относится к if(...) => отступ как у лейбла выше

    switch(1) {
        case 1 ? 1 : 0: // Тернарный оператор в метке
            printf("hello\n");
        case 1 ? 1
               : 0: {
            printf("hello\n");
        }
        case 1 ? 1 : 0: {
            printf("hello\n");
        }
        default:
            printf("");
    }
}
