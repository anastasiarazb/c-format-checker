int main(void)
{
    if(i < n)
        diff = diff*x+a;
    if (1 ? 1 : 0)
        if1  // Лейбл и последующее выражение - одно утверждение
          :  // перенос
        printf("if 1 printf1\n");  // printf(...) относится к if(...) => отступ как у лейбла выше
    case 1 ? 1 : 0:
        printf("hello\n");
    case 1 ? 1
    : 0: {
            printf("hello\n");
        }
    case 1 ? 1 : 0: {
        printf("hello\n");
    }
    default:
        printf("");
}
